# 待办 / 已知问题

> 本文件记录已知但暂未处理的问题，按发现时间倒序。修复后把条目移到「已修复」或删除。

## 待修

### [2026-06-26] 结算页「完整版收录」是误导性占位，点不开 ⚠️ 优先

**现象**：当班结束的结算页（report screen）有一块「完整版收录 / In the full edition」，列了 4 条"完整版"功能：
1. 八个班次——覆盖老年、学生、小微企业等柜台
2. 60+ 真实诈骗剧本：深伪语音、收款码掉包、假冒客服专线
3. 职业生涯模式：骗术随你的打法自适应进化
4. 一个可免费分享给家人的开放案例库

这些内容**并不存在、也点不开**，是 MVP 阶段留下的"完整版预告"占位。现在游戏已是 300+ 案件、可正式上线游玩的完整 demo，再挂一个看着像"锁住/即将上线"却哪也去不了的板块，会让玩家以为有内容没加载出来 / 功能坏了，属于误导。

**定位**：
- 文案：`script.js` 内 `L.en` / `L.zh` 的 `lockTitle` + `lockItem1`~`lockItem4`（当前约 44–48 / 82–86 行，以字符串标识为准）。
- 结构：`index.html` `#reportScreen .contents`（`lockTitle` + 4 个 `lockItem` 的 `<li>`）。
- 渲染：`renderReport()`（结算页）。

**可选修法（明天再定）**：
- A. 直接删掉这块"完整版收录"，结算页只留成绩 + mission + 再抽一班。
- B. 换成**诚实内容**：本 demo 已收录 300+ 真实案件 / 中英双语 / 双轴信任机制 / 开放可分享——把"画饼"改成"已实现"，且不做成锁住样式。
- C. 若要保留 roadmap 预告，改文案为"后续计划"并去掉"锁住/可查看"的视觉暗示，明确它是规划而非缺失内容。
- 备注：英文 `L.en` 同步改。

**约束**：本轮先记录，不改游戏内容；小红书暂不发布，待此问题修复后再发。
