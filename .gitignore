# Local preview / editor config (contains machine-specific absolute paths)
.claude/

# build artifacts (upload packages)
*.zip

# OS / editor cruft
Thumbs.db
.DS_Store
*.log

# _video pipeline: deps, renders, captures, secrets (kept: code + small voiceover)
_video/node_modules/
_video/out/
_video/.env
_video/public/capture/
_video/public/audio/_*
